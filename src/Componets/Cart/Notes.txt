We added the package.json file so that we dont have to go thorugh every link in the code since we moved cart.js into a folder named cart

This makes is easer if we just add json file and add the main file to cart